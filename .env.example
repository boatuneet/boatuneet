# Supabase — project settings > API. Both are server-only: no NEXT_PUBLIC_
# prefix, so Next never ships them to the browser.
# Copy to .env.local (gitignored) and fill in.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
